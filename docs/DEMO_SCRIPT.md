# Demo Script (≤ 5 minutes)

_Fill screenshots into `docs/images/` on Day 6–7._

1. **Problem (20 s)** — Large logs, sequential analysis is slow; HPC is the primary engine.
2. **Architecture (40 s)** — System A CLI vs System B product layer; four abstractions.
3. **Upload + run (60 s)** — New Analysis: file, format, workers, Run. Show job `queued → running → aggregating → completed`.
4. **Results (60 s)** — Totals, errors, top services, security findings (evidence, not “definitely an attack”).
5. **Benchmark chart (60 s)** — Workers vs time / speedup / efficiency on i5-1235U. Mention sublinear scaling.
6. **AI report (40 s)** — Generate from aggregates only. Show Ollama-down fallback if useful.
7. **Correctness (20 s)** — Sequential ≡ parallel. CLI still works without the web UI.
8. **Close (20 s)** — GitHub Issues/Projects + PERFORMANCE.md honesty.

# Pin exact versions after first successful install.
pytest>=8.0
# ruff

# Day 5+
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
pydantic>=2.0
python-multipart>=0.0.9
httpx>=0.27
PyJWT>=2.9

# Stage 2 — MPI (requires system MPI: MS-MPI on Windows or OpenMPI on WSL/Linux)
mpi4py>=4.0
